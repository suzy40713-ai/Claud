# Uniquement necessaire pour l'app mobile packagee (Capacitor) : le webview
# n'a pas de backend sur la meme origine. Doit pointer vers le backend
# deploye en HTTPS. Laisser vide pour le web (proxy Vite en dev, meme
# origine en prod).
VITE_API_URL=
